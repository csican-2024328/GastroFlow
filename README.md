# GastroFlow

Esqueleto backend basado en el repositorio de referencia para `Node-AuthService-IN6B`.

Instrucciones rápidas:

```bash
pnpm install
pnpm run dev
```

Configurar `.env` con `MONGODB_URI` y `SECRET_KEY` antes de ejecutar.
